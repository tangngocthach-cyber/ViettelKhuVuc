<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Truy cập mạng để gọi API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Chụp ảnh gửi Chat -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Định vị GPS để chia sẻ vị trí trong Chat -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Ghi âm tin nhắn thoại trong Chat -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Thông báo hẹn giờ (Nhắc hẹn trong Chat) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Đọc ảnh/file từ thư viện để gửi Chat (Android 13+ dùng quyền chi tiết hơn) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- QUAN TRỌNG: cho phép app tự cài bản cập nhật .apk tải về (vì không phát hành
         qua Google Play) - nếu thiếu quyền này, Android sẽ chặn cài đặt im lặng -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Bắt buộc từ Android 11+ (API 30): khai báo rõ app CÓ THỂ mở trình duyệt
         ngoài (VD khi WebView gặp link ra ngoài site như NotebookLM), nếu thiếu
         khai báo này, launchUrl() sẽ báo không tìm thấy app xử lý được link -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:label="Viettel Khu Vực Vĩnh Hưng"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data android:name="flutterEmbedding" android:value="2" />
        <!-- Icon/màu mặc định cho thông báo đẩy Firebase - tránh hiện icon
             trắng/vuông mặc định của Android trên một số máy -->
        <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@mipmap/ic_launcher" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_color" android:resource="@color/notification_color" />

        <!-- FileProvider: bắt buộc để mở/cài file .apk vừa tải về (Android 7+ chặn
             chia sẻ đường dẫn file:// trực tiếp giữa các app vì lý do bảo mật) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
